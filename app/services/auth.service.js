module.exports = {
    isLogin: userId => {
        // Check if the user is login
        return true;
    },
    isValidToken: token => {
        // Check if token is valid
        return true;
    }
}